# macOS & Xcode
.DS_Store
*.swp
*.lock

# Xcode user data and state
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Derived data and build artifacts
DerivedData/
build/
dist/

# SwiftPM
.swiftpm/
.build/
Package.resolved

# Carthage/CocoaPods (if used later)
Carthage/
Pods/

# Fastlane (if used later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# App-specific: do not commit the internal roadmap
# (explicit file in this repo root)
dialed-in-ui-roadmap.md

# Local tooling
.claude/
